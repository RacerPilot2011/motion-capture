# Motion Capturing Software
### A simple scipt that takes webcam or pre recorded video and outputs a .bvh file

To be used in a game
## Running

Simple put, do ```git clone https://github.com/RacerPilot2011/motion-capture.git``` to clone the repo then run ```npm i | node index.js``` to start the app on port 3000
